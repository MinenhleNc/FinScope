<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Main App Theme (Pink & Blank) -->
    <style name="Theme.FinScope" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Pink Primary Colors -->
        <item name="colorPrimary">@color/pink_500</item>
        <item name="colorPrimaryVariant">@color/pink_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- White Background for Blank Look -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/pink_700</item>
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Match Splash Screen Settings -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
    </style>

    <!-- Splash Theme (Kept as Original) -->
    <style name="Theme.FinScope.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>

